export interface FileValidator {
    validate(files: File[]): boolean;
    filterFiles(files: File[]): File[];
    acceptString?: string;
}
export declare class NullFileValidator implements FileValidator {
    validate(files: File[]): boolean;
    filterFiles(files: File[]): File[];
}
export declare class FileExtensionValidator implements FileValidator {
    constructor(validExtensions?: string[]);
    validate(files: File[]): boolean;
    filterFiles(files: File[]): File[];
    acceptString: string;
    extensions: string[];
    private extRE;
    private checkExt(name);
    private checkFile(f);
}
